# library-book-lending-system-jdbc
Java JDBC-based Library Book Lending System that manages book records, issue and return transactions, and inventory control. It supports add/view/issue/return/remove book operations with transactional processing, DAO architecture, and custom exception handling for validation, stock limits, and data integrity.
